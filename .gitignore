# Build output and installed packages (regenerated by npm ci / npm run build).
# The Android copy of the built app (android/app/src/main/assets/public) is
# deliberately still tracked; see the README.
node_modules/
/dist/

# Local / OS clutter
*.log
.DS_Store
